# 待办

只记录尚未完成的工作；完成后更新权威文档并移除已完成清单。当前行为以代码、测试和工具 README 为准。

## CLI-01 新增工具的 CLI

状态：待实施。本轮只整理入口和契约，不新增命令。

- 范围：Master → 小表、小表 → Master、列操作、兼容性重存、同名文件替换、未翻译统计。
- 业务直接复用现有处理器，位置见[仓库地图](repository-map.md#工作流与工具)。命令名、子命令结构尚未确定，不能当作可用命令调用。
- 提供完整非交互参数、逐项 `--help`、输出摘要和明确的失败/部分失败返回值。
- 保持 GUI 对应业务契约：可选输出默认原位；空白不转文字 None/nan；同步与统计的 nan 口径各自保留；保存失败不覆盖原文件；替换先备份；COM 隔离与清理。
- Windows/Excel 依赖仅在执行需要 COM 的动作时加载，帮助命令及其他工具不要求 Excel。

验收：六项从 CLI 可运行、与业务层/GUI 使用相同测试样本结果一致；补 `COMMANDS`、别名唯一性、参数转发、错误退出码、无 Excel 环境帮助测试；更新 [CLI 手册](cli-usage.md) 和工具 README；全量回归通过。

## DOC-01 说明网页同步

状态：待实施。目标目录：[qa-workflow-guide](qa-workflow-guide/index.html)；仓库相对路径 `docs/qa-workflow-guide/`。

此处“内容同步”指让说明网页与当前产品保持一致，并补充内容同步工具的使用入口。

- 更新导航截图及名称：常用流程、文件与表格、翻译辅助。
- 补充内容同步双 Tab、另外四项新工具、可选原位输出、简约实时日志。
- 更新 QA 的表头别名优先级、记住选项、问题排序、修订校验与术语问题状态说明。
- 截图与文案匹配当前简约界面；历史截图不可继续充当当前布局。
- 业务细则链接工具 README，避免网页再维护一套容易漂移的规则。

验收：从产品实际页面核对每一步、更新截图、检查相对资源/锚点、离线打开网页无丢图；桌面与窄窗口可读。未完成前，网页仅作旧版操作参考，当前规则请按[文档索引](README.md)查询。
