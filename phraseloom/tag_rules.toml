version = 1

[angle_tags]
mode = "allowlist"
allowed = ["color", "size", "img", "br", "i", "u", "outline", "c"]

[angle_tags.aliases]
c = "color"

[angle_tags.single]
tags = ["img", "br"]

[angle_tags.optional_pair]
tags = ["i", "outline"]

[bbcode_tags]
mode = "allowlist"
allowed = ["color", "b", "i", "u", "size"]

[raw_braces]
protect_all = true
